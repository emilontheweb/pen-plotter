# Virtual environment
venv/

# Python cache
__pycache__/
*.pyc

# Input images (copyrighted album art — kept local)
images/*
# ...except the generated, copyright-free demo image (safe to commit)
!images/test_pattern.png

# Generated output (SVG/G-code we'll produce later)
output/
*.gcode

# OS / editor cruft
.DS_Store
.vscode/

# Claude Code local settings (personal, not shared)
.claude/settings.local.json

# Internal project brief (kept local, not for public repo)
CLAUDE.md